node_modules/

# Derleme çıktıları. `my-registry/dist/registry.json` build script'inden üretilir;
# depoya konmaz, `bun run registry:build` ile yeniden kurulur.
dist/
dist-viewer/

# `bun run render` ve `bun run export:glb` çıktıları. İkisi de kaynaktan
# yeniden üretiliyor; depoda tutmak birkaç megabaytlık ikili dosyayı her
# değişiklikte yeniden yazmak demek olurdu.
renders/
glb/

*.log
*.tgz
*.tsbuildinfo
.DS_Store

# Ajan/araç durumu
.omc/
.claude/settings.local.json

# models.lock.json KASITLI OLARAK dahil edilir: kurulu kaynağın hangi sürümden
# geldiğini kaydeder ve `vibe3d diff` onunla yerel değişiklikleri bulur.
scripts/probe.ts
scripts/zdiag.ts
